FROM postgres:17-alpine

# Install pgvector from Alpine packages (provides the vector extension library)
RUN apk add --no-cache postgresql-pgvector

# Do NOT preload vector. It is not required and can break initdb if the library path differs.
# Preloading is disabled here; extensions will be created via init scripts.

# The rest of the configuration will be handled by docker-compose
