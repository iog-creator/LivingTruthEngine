#!/usr/bin/env bash
set -euo pipefail
MSG_FILE="$1"
if ! grep -q "^\[SSOT Verified\]" "$MSG_FILE"; then
  echo "✗ Commit message must start with [SSOT Verified]" >&2
  echo "  Hint: run SSOT checks, then prefix your message." >&2
  exit 1
fi
